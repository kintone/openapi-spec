properties:
  includeSubs:
    type: boolean
  entity:
    properties:
      code:
        type: string
      type:
        enum:
          - USER
          - GROUP
          - ORGANIZATION
          - FIELD_ENTITY
        type: string
    required:
      - type
    type: object
required:
  - entity
type: object
