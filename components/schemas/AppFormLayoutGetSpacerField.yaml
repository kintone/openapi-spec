properties:
  elementId:
    maxLength: 64
    type: string
  size:
    properties:
      width:
        format: int64
        type: integer
      height:
        format: int64
        type: integer
    type: object
  type:
    enum:
      - CALC
      - CHECK_BOX
      - CREATED_TIME
      - CREATOR
      - DATE
      - DATETIME
      - DROP_DOWN
      - FILE
      - HR
      - LABEL
      - LINK
      - MODIFIER
      - MULTI_LINE_TEXT
      - MULTI_SELECT
      - NUMBER
      - RADIO_BUTTON
      - RECORD_NUMBER
      - REFERENCE_TABLE
      - RICH_TEXT
      - SINGLE_LINE_TEXT
      - SPACER
      - TIME
      - UPDATED_TIME
      - USER_SELECT
      - ORGANIZATION_SELECT
      - GROUP_SELECT
    type: string
required:
  - type
type: object
