properties:
  relatedField:
    type: string
    description: |-
      The field code of the field set for the mapping source.
      Required if setting the `lookup.fieldMappings[]` parameter.
  field:
    type: string
    description: |-
      The field code of the field set for the mapping endpoint.
      Required if setting the `lookup.fieldMappings[]` parameter.
type: object
