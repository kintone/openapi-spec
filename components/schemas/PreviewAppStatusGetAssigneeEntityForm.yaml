properties:
  includeSubs:
    type: boolean
  entity:
    properties:
      code:
        type: string
      type:
        enum:
          - USER
          - GROUP
          - ORGANIZATION
          - FIELD_ENTITY
          - CREATOR
          - CUSTOM_FIELD
        type: string
    type: object
type: object
