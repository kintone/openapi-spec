properties:
  noLabel:
    type: boolean
  expression:
    type: string
  code:
    type: string
  defaultValue:
    type: string
  minLength:
    format: int64
    type: integer
  unique:
    type: boolean
  label:
    type: string
  type:
    enum:
      - LABEL
      - SPACER
      - HR
      - CREATED_TIME
      - CREATOR
      - DATE
      - DATETIME
      - NUMBER
      - CALC
      - RICH_TEXT
      - FILE
      - UPDATED_TIME
      - MODIFIER
      - CHECK_BOX
      - MULTI_LINE_TEXT
      - MULTI_SELECT
      - RECORD_NUMBER
      - RADIO_BUTTON
      - SINGLE_LINE_TEXT
      - DROP_DOWN
      - SUBTABLE
      - TIME
      - USER_SELECT
      - ORGANIZATION_SELECT
      - GROUP_SELECT
      - REFERENCE_TABLE
      - LINK
    type: string
  maxLength:
    format: int64
    type: integer
  hideExpression:
    type: boolean
  required:
    type: boolean
type: object
