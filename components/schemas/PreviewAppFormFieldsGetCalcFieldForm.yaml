properties:
  noLabel:
    format: boolean
    type: boolean
  unit:
    maxLength: 128
    type: string
  expression:
    type: string
  code:
    maxLength: 128
    minLength: 1
    type: string
  format:
    enum:
      - NUMBER
      - NUMBER_DIGIT
      - DATETIME
      - DATE
      - TIME
      - HOUR_MINUTE
      - DAY_HOUR_MINUTE
    type: string
  unitPosition:
    enum:
      - BEFORE
      - AFTER
    type: string
  label:
    maxLength: 128
    minLength: 1
    type: string
  type:
    enum:
      - CALC
      - CATEGORY
      - CHECK_BOX
      - CREATED_TIME
      - CREATOR
      - DATE
      - DATETIME
      - DROP_DOWN
      - FILE
      - GROUP
      - GROUP_SELECT
      - LINK
      - MODIFIER
      - MULTI_LINE_TEXT
      - MULTI_SELECT
      - NUMBER
      - ORGANIZATION_SELECT
      - RADIO_BUTTON
      - RECORD_NUMBER
      - REFERENCE_TABLE
      - RICH_TEXT
      - SINGLE_LINE_TEXT
      - STATUS
      - STATUS_ASSIGNEE
      - SUBTABLE
      - TIME
      - UPDATED_TIME
      - USER_SELECT
    type: string
  displayScale:
    format: int64
    type: integer
  hideExpression:
    format: boolean
    type: boolean
  required:
    format: boolean
    type: boolean
required:
  - type
type: object
